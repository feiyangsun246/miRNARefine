% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_inputCheckGeneral.R
\name{inputCheckGeneral}
\alias{inputCheckGeneral}
\title{Check and standardize input miRNA data}
\usage{
inputCheckGeneral(miRNAdata)
}
\arguments{
\item{miRNAdata}{Input dataset (mostly a matrix or data frame) containing miRNA expression values.}
}
\value{
A data frame with numeric columns after necessary conversions.
}
\description{
This function validates and standardizes the input miRNA expression data.
It ensures the data is a matrix or data frame, is non-empty, and converts
factor columns to numeric.
}
\details{
The function performs the following checks:
\itemize{
\item Ensures the input is a matrix or data frame.
\item Stops with an error if the input is empty (no rows or columns).
\item Converts factor columns to numeric via character conversion.
}
}
\examples{
# Example 1:
# Input a dataframe
df <- data.frame(a = factor(c("1", "2")), b = c(3, 4))
res1 <- inputCheckGeneral(df)
class(res1) # "data.frame"

# Example 2:
# Input a matrix
mat <- matrix(1:4, nrow = 2)
res2 <- inputCheckGeneral(mat)
class(res2) # "data.frame"

# Example 3:
# Use dataset provided in the package
data(miRNASeq1)
res3 <- inputCheckGeneral(miRNASeq1)
class(res3) # "data.frame"
dim(res3) # 10 18

}
